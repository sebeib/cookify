meta {
  name: register user with invite
  type: http
  seq: 2
}

post {
  url: {{host}}/api/invite/{{inviteId}}
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "username": "jane.doe",
    "password": "super-secret-password",
    "displayName": "Jane Doe"
  }
}
